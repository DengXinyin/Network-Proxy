# 服务器网络与代理排查指南（Mihomo / Clash Verge / Codex / OpenAI 优化版）

> 适用环境：node4 服务器，用户 `xydeng`（uid 1012）  
> 最后更新：2026-09-21  
> 目的：下次遇到“连不上 ChatGPT / API 不通 / Codex 断流 / VS Code 扩展异常”，优先按本文自检，不再从头排查。

---

## 一、当前推荐架构

### 1. 最终稳定方案

当前推荐：

```text
Windows
└─ Clash Verge / FlyBit / 其他桌面代理
   └─ 仅负责 Windows 本机

Linux 服务器
└─ 127.0.0.1:5964
   └─ xydeng 自己的 mihomo
      ├─ OpenAI / ChatGPT 流量
      │  └─ OpenAI
      │     └─ ChatGPT-Auto
      │        └─ 仅 US / JP 节点
      │           └─ 用 OpenAI API 做健康检查
      │
      └─ 其他流量
         └─ PERSONAL_PROXY
            └─ 用通用测速地址自动选节点
```

当前正式代理端口：

```text
127.0.0.1:5964
```

当前已经不再依赖：

```text
服务器
→ SSH RemoteForward
→ Windows
→ FlyBit
```

这种方案虽然普通 `curl`、TLS、短请求可以正常，但 Codex 的 HTTPS streaming 曾出现：

```text
stream disconnected before completion
error sending request for url
https://chatgpt.com/backend-api/codex/responses
```

本地 Mihomo + OpenAI 专用分流策略经过实际测试更稳定。

---

## 二、理解本机配置

### 1. Shell 加载链

```text
~/.bash_profile  →  source ~/.bashrc
~/.profile       →  source ~/.bashrc
```

VS Code Remote Server 也应能继承相同代理环境。

当前代理环境统一从 `~/.bashrc` 加载。

### 2. 当前关键环境变量

```bash
# 代理
export http_proxy='http://127.0.0.1:5964'
export https_proxy='http://127.0.0.1:5964'
export HTTP_PROXY="$http_proxy"
export HTTPS_PROXY="$https_proxy"

# 避免 ALL_PROXY 干扰
unset ALL_PROXY all_proxy

# 不走代理的域名
export no_proxy="api.deepseek.com"
export NO_PROXY="localhost,127.0.0.1,::1,api.deepseek.com"
```

如果还要使用 Claude Code + DeepSeek：

```bash
export ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
export ANTHROPIC_AUTH_TOKEN="sk-..."
export ANTHROPIC_MODEL="deepseek-v4-flash"
export ANTHROPIC_DEFAULT_OPUS_MODEL="deepseek-v4-flash"
export ANTHROPIC_DEFAULT_SONNET_MODEL="deepseek-v4-flash"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="deepseek-v4-flash"
```

> 注意：真实 Token 不要写进公开 GitHub 文档。

---

## 三、先理解端口：同一个数字，不代表同一个代理

下面两个端口虽然都叫 `5964`，但在不同机器上：

```text
Windows 127.0.0.1:5964
Linux   127.0.0.1:5964
```

`127.0.0.1` 永远代表“当前这台机器自己”。

因此：

```bash
curl -x http://127.0.0.1:5964 https://example.com
```

在 Linux 服务器上执行时，访问的是**服务器自己的 5964**，不是 Windows 的 5964。

历史上使用过：

```sshconfig
RemoteForward 17897 127.0.0.1:5964
```

其含义是：

```text
服务器 127.0.0.1:17897
        ↓ SSH
Windows 127.0.0.1:5964
```

也测试过：

```sshconfig
RemoteForward 5964 127.0.0.1:5964
```

即：

```text
服务器 5964
   ↓ SSH
Windows 5964
```

但当前正式方案已经不使用 RemoteForward。

---

## 四、Windows SSH 配置（当前推荐）

当前 `C:\Users\HUITIAN\.ssh\config`：

```sshconfig
Host 192.168.30.206
  HostName 192.168.30.206
  User xydeng
  Port 22

Host 192.168.30.205
  HostName 192.168.30.205
  User xydeng
  Port 22
  ServerAliveInterval 30
  ServerAliveCountMax 3
  # IdentityFile C:/Users/HUITIAN/.ssh/id_rsa
```

保持以下内容删除或注释：

```sshconfig
# RemoteForward 5964 127.0.0.1:5964
# ExitOnForwardFailure yes
```

历史上的：

```sshconfig
RemoteForward 17897 127.0.0.1:5964
```

也不再需要。

---

## 五、快速自检（以后优先跑这一套）

### 1. 环境变量

```bash
echo "HTTP_PROXY=$HTTP_PROXY"
echo "HTTPS_PROXY=$HTTPS_PROXY"
env | grep -i proxy
```

预期：

```text
HTTP_PROXY=http://127.0.0.1:5964
HTTPS_PROXY=http://127.0.0.1:5964
```

### 2. 5964 谁在监听

```bash
ss -lntp | grep ':5964'
```

预期：

```text
127.0.0.1:5964 ... users:(("mihomo",pid=...,fd=...))
```

如需更明确：

```bash
sudo lsof -nP -iTCP:5964 -sTCP:LISTEN
```

或者：

```bash
sudo ss -lntp | grep ':5964'
```

### 3. 进程是谁

```bash
ps -fp <PID>
```

例如：

```bash
ps -fp 3299495
```

### 4. 普通代理是否可用

```bash
curl -x http://127.0.0.1:5964 -s https://api.ipify.org
```

或：

```bash
curl -x http://127.0.0.1:5964 -s https://ifconfig.me/ip
```

### 5. OpenAI API 是否可达

```bash
curl -i https://api.openai.com/v1/models
```

如果返回：

```text
HTTP/2 401
Missing bearer authentication
```

说明网络和 TLS 已经到达 OpenAI，只是没带 API Key。

### 6. ChatGPT Codex endpoint 是否可达

```bash
curl -v -x http://127.0.0.1:5964 \
  --http1.1 \
  https://chatgpt.com/backend-api/codex/responses
```

GET 请求常见返回：

```text
405 Method Not Allowed
allow: POST
```

这说明：

- HTTP CONNECT 成功；
- TLS 握手成功；
- ChatGPT 后端可达。

但这**不能证明真正的 Codex streaming 一定稳定**。

---

## 六、Mihomo 服务管理

### 1. 当前程序路径

```text
/home/xydeng/.local/bin/mihomo
```

### 2. 当前配置目录

```text
/home/xydeng/.config/mihomo
```

### 3. systemd 用户服务

```text
/home/xydeng/.config/systemd/user/mihomo.service
```

运行命令：

```text
/home/xydeng/.local/bin/mihomo -d /home/xydeng/.config/mihomo
```

### 4. 查看状态

```bash
systemctl --user status mihomo
```

正常应看到：

```text
Loaded: loaded (...; enabled)
Active: active (running)
```

### 5. 常用命令

```bash
systemctl --user start mihomo
systemctl --user stop mihomo
systemctl --user restart mihomo

systemctl --user enable mihomo
systemctl --user disable mihomo

systemctl --user enable --now mihomo
systemctl --user daemon-reload
```

---

## 七、为什么原来的 Mihomo 策略不适合 Codex

原来的核心配置类似：

```yaml
proxy-groups:
  - name: PERSONAL_PROXY
    type: url-test
    use:
      - personal_subscription
    url: https://www.gstatic.com/generate_204
    interval: 300
    tolerance: 80

rules:
  - MATCH,PERSONAL_PROXY
```

实际逻辑：

```text
所有网站
  ↓
所有订阅节点一起参加
  ↓
用 Google generate_204 测延迟
  ↓
选择“通用网络最快”的节点
```

问题是：

> 对 Google 最快，不代表对 OpenAI / ChatGPT / Codex 最稳定。

实际排障中出现过：

- `curl` 正常；
- 普通网页正常；
- TLS 正常；
- 但 Codex 403 / WebSocket 异常 / HTTPS streaming 中断；
- 自动选择曾落到香港方向出口。

---

## 八、OpenAI / Codex 专用代理优化方案

### 1. 核心策略

把 OpenAI 流量单独分流：

```text
chatgpt.com / openai.com / oaistatic.com / ...
        ↓
OpenAI
        ↓
ChatGPT-Auto
        ↓
只允许 US / JP 节点
        ↓
用 OpenAI API 地址实际测速
```

其他网站继续走：

```text
PERSONAL_PROXY
→ Google generate_204
```

### 2. 推荐 `config.yaml`

> 不要把真实订阅 URL、UUID、token、密码、私钥提交到 GitHub。

```yaml
mixed-port: 5964
allow-lan: false
bind-address: 127.0.0.1
mode: rule
log-level: info
ipv6: false

external-controller-unix: /home/xydeng/.config/mihomo/api.sock

proxy-providers:
  personal_subscription:
    type: http
    url: "<YOUR_SUBSCRIPTION_URL>"
    path: ./providers/personal_subscription.yaml
    interval: 3600

    health-check:
      enable: true
      interval: 300
      timeout: 5000
      url: https://www.gstatic.com/generate_204

proxy-groups:

  # OpenAI 专用自动选择
  - name: ChatGPT-Auto
    type: url-test
    use:
      - personal_subscription

    # 只筛美国、日本节点
    filter: '(?i)(美国|日本|united states|japan|(^|[^a-z])(us|jp)([^a-z]|$))'

    # 用 OpenAI API 实际检查
    url: https://api.openai.com/v1/models

    interval: 300
    tolerance: 150
    lazy: false

  # 普通网络自动组
  - name: PERSONAL_PROXY
    type: url-test
    use:
      - personal_subscription
    url: https://www.gstatic.com/generate_204
    interval: 300
    tolerance: 80
    lazy: false

  # OpenAI 路由入口
  - name: OpenAI
    type: select
    proxies:
      - ChatGPT-Auto
      - PERSONAL_PROXY

rules:
  - DOMAIN,chatgpt.com,OpenAI
  - DOMAIN,auth.openai.com,OpenAI
  - DOMAIN-SUFFIX,chatgpt.com,OpenAI
  - DOMAIN-SUFFIX,openai.com,OpenAI
  - DOMAIN-SUFFIX,oaistatic.com,OpenAI
  - DOMAIN-SUFFIX,oaiusercontent.com,OpenAI
  - DOMAIN-SUFFIX,statsigapi.net,OpenAI

  - MATCH,PERSONAL_PROXY
```

---

## 九、为什么 `api.ipify.org` 可能显示香港，但 Codex 仍正常

这是分流策略生效后的正常现象。

例如：

```bash
curl -x http://127.0.0.1:5964 -s https://api.ipify.org
```

可能返回：

```text
143.246.51.3
```

因为：

```text
api.ipify.org
```

不属于 OpenAI 域名，所以走：

```text
PERSONAL_PROXY
```

而：

```text
chatgpt.com
api.openai.com
auth.openai.com
```

会走：

```text
OpenAI
→ ChatGPT-Auto
→ US / JP
```

所以**普通网站出口和 OpenAI 出口可以不同**。

---

## 十、推荐的测试流程：先用 5965，不直接动 5964

修改正式配置前，先做一套测试目录。

### 1. 复制

```bash
cp -a ~/.config/mihomo ~/.config/mihomo-test
```

### 2. 测试端口

把测试配置改成：

```yaml
mixed-port: 5965
```

### 3. 前台启动

```bash
~/.local/bin/mihomo -d ~/.config/mihomo-test
```

### 4. 查看监听

```bash
ss -lntp | grep ':5965'
```

### 5. 测试联网

```bash
curl -x http://127.0.0.1:5965 -s https://api.ipify.org
```

### 6. 当前 Shell 临时切到 5965

```bash
export HTTP_PROXY=http://127.0.0.1:5965
export HTTPS_PROXY=http://127.0.0.1:5965
export http_proxy=http://127.0.0.1:5965
export https_proxy=http://127.0.0.1:5965
```

### 7. 测 Codex

```bash
codex
```

输入：

```text
hello
```

如果连续回复正常，再迁移到正式 5964。

---

## 十一、从测试配置正式迁移到 5964

### 1. 备份

```bash
cp ~/.config/mihomo/config.yaml \
   ~/.config/mihomo/config.yaml.bak.before-openai-$(date +%Y%m%d-%H%M%S)
```

### 2. 测试配置改回 5964

```bash
sed -i 's/^mixed-port: 5965$/mixed-port: 5964/' \
~/.config/mihomo-test/config.yaml
```

### 3. 覆盖正式配置

```bash
cp ~/.config/mihomo-test/config.yaml ~/.config/mihomo/config.yaml
```

### 4. 确保 SSH 不再占用 5964

Windows SSH config 中不应存在活动的：

```sshconfig
RemoteForward 5964 127.0.0.1:5964
```

服务器检查：

```bash
ss -lntp | grep ':5964'
```

启动 mihomo 前应为空。

### 5. 正式启动

```bash
systemctl --user daemon-reload
systemctl --user enable --now mihomo
```

### 6. 验证

```bash
systemctl --user status mihomo
ss -lntp | grep ':5964'
```

预期：

```text
127.0.0.1:5964 ... mihomo
```

---

## 十二、Codex CLI 版本与路径排查

### 1. 当前版本

```bash
codex --version
```

当前正式使用：

```text
codex-cli 0.155.1
```

### 2. 当前命令位置

```bash
which codex
```

预期：

```text
~/.local/bin/codex
```

### 3. 查看真实可执行文件

```bash
readlink -f "$(which codex)"
```

当前：

```text
/home/xydeng/.codex/packages/standalone/releases/0.155.1-x86_64-unknown-linux-musl/bin/codex
```

### 4. 是否有多套 Codex

```bash
type -a codex
```

历史上曾临时安装 npm 版 `0.149.1` 做 A/B 测试：

```bash
npm install -g @openai/codex@0.149.1
```

测试后确认旧版仍然断流，因此排除“单纯版本回归”。

随后已卸载：

```bash
npm uninstall -g @openai/codex
```

现在应只剩 standalone `0.155.1`。

---

## 十三、VS Code Codex 扩展：一定要查“进程真实环境”

终端里的代理环境正确，不代表已经运行的 VS Code Codex 扩展也正确。

### 1. 找 Codex 扩展进程

```bash
ps -ef | grep 'openai.chatgpt.*codex' | grep xydeng | grep -v grep
```

典型路径：

```text
~/.vscode-server/extensions/openai.chatgpt-*/bin/linux-x86_64/codex
```

### 2. 查看该进程真正继承的环境变量

```bash
tr '\0' '\n' < /proc/<PID>/environ | \
grep -iE 'http_proxy|https_proxy|all_proxy|no_proxy'
```

预期：

```text
HTTP_PROXY=http://127.0.0.1:5964
HTTPS_PROXY=http://127.0.0.1:5964
http_proxy=http://127.0.0.1:5964
https_proxy=http://127.0.0.1:5964
```

如果仍是旧端口，重新启动 VS Code Remote-SSH / VS Code Server。

---

## 十四、Codex doctor 与 WebSocket

### 1. 检查

```bash
codex doctor --json
```

重点看：

```text
network.provider_reachability
network.websocket_reachability
proxy env vars present
model provider
```

### 2. 曾做过的 HTTP-only 对照测试

曾配置：

```toml
model_provider = "chatgpt-http"

[model_providers.chatgpt-http]
name = "ChatGPT HTTP"
base_url = "https://chatgpt.com/backend-api/codex"
wire_api = "responses"
requires_openai_auth = true
supports_websockets = false
```

doctor 显示：

```text
Responses WebSocket is not enabled for the active provider
supports websockets: false
```

但旧的 SSH RemoteForward 链路仍然出现 HTTPS streaming 中断。

因此本次问题不是单纯 WebSocket。

---

## 十五、本次最关键的 A/B 对照实验

### A. SSH RemoteForward → Windows FlyBit

```text
Codex
→ 服务器 5964
→ SSH RemoteForward
→ Windows FlyBit 5964
→ OpenAI
```

结果：

```text
普通 curl：正常
TLS：正常
GET /backend-api/codex/responses：405（说明可达）
Codex streaming：失败
```

典型错误：

```text
stream disconnected before completion
error sending request for url
https://chatgpt.com/backend-api/codex/responses
```

### B. 服务器另一个本地 Mihomo

在服务器直接使用另一个已存在的 Mihomo 代理端口做临时测试时：

```text
Codex → 本地 mihomo → Internet
```

Codex 正常回复。

这个实验说明：

```text
Codex 本身正常
Linux 主机正常
ChatGPT 登录正常
0.155.1 版本正常
```

问题集中在原有的：

```text
SSH RemoteForward + Windows 代理
```

额外链路。

### C. 自己的 Mihomo + OpenAI 专用路由

先在：

```text
127.0.0.1:5965
```

测试 OpenAI 专用路由。

结果：

```text
Codex hello → 正常回复
连续对话 → 正常
```

随后迁回：

```text
127.0.0.1:5964
```

仍然正常。

这就是当前最终方案。

---

## 十六、DNS 与 Cloudflare 排查

### 1. DNS 检查

```bash
nslookup chatgpt.com
dig chatgpt.com +short
```

但不要仅凭“是否落在某些固定 Cloudflare IP 段”判断 DNS 是否污染，因为 CDN 地址会变化。

更可靠的判断方式是：

- 是否能通过代理正常完成 TLS；
- 是否能拿到正确证书；
- 是否能访问 OpenAI API；
- 是否能完成实际 Codex 请求。

### 2. Cloudflare 403

```bash
curl -sI --proxy http://127.0.0.1:5964 https://chatgpt.com
```

如果看到：

```text
403
cf-mitigated: challenge
```

通常表示 Cloudflare 要浏览器挑战。

这和：

```text
/api/openai.com/v1/models → 401
```

不是一回事。

### 3. HKG / SJC 只代表 Cloudflare PoP

例如：

```text
CF-RAY: ...-HKG
CF-RAY: ...-SJC
```

这里只能说明请求进入了哪个 Cloudflare PoP，不能单独作为“你的公网出口一定在香港/圣何塞”的证据。

判断真实出口应优先：

```bash
curl -x http://127.0.0.1:5964 -s https://api.ipify.org
```

---

## 十七、错误状态码速查

| 状态 / 错误 | 含义 |
|---|---|
| `Connection refused` | 端口没人监听 |
| `remote port forwarding failed` | SSH RemoteForward 远程端口被占用或无法绑定 |
| `401 Missing bearer authentication` | OpenAI API 可达，但没带认证 |
| `403 + cf-mitigated: challenge` | Cloudflare 浏览器挑战 |
| `405 Method Not Allowed` | 请求已到 Codex endpoint，但 GET 不被允许 |
| `429 Rate limit hit` | 测试网站本身限流，不等于代理故障 |
| `stream disconnected before completion` | 长连接 / streaming 中途断开 |
| 终端可用、扩展不可用 | 常见于 VS Code 扩展继承旧代理环境 |

---

## 十八、常用操作

### 1. 重新加载环境变量

```bash
source ~/.bashrc
```

### 2. 临时关代理

```bash
unset HTTP_PROXY HTTPS_PROXY http_proxy https_proxy ALL_PROXY all_proxy
```

### 3. 临时切换代理端口

```bash
export HTTP_PROXY=http://127.0.0.1:5964
export HTTPS_PROXY=http://127.0.0.1:5964
export http_proxy=http://127.0.0.1:5964
export https_proxy=http://127.0.0.1:5964
```

### 4. 指定 URL 走代理

```bash
curl -sI --proxy http://127.0.0.1:5964 https://目标网址 | head -3
```

### 5. 不走代理

```bash
curl -sI --noproxy '*' https://目标网址 | head -3
```

### 6. Python requests

```python
import requests

# 默认会读取 http_proxy / https_proxy
r = requests.get("https://api.openai.com/v1/models")

# 或手工指定
r = requests.get(
    "https://api.openai.com/v1/models",
    proxies={"https": "http://127.0.0.1:5964"},
)
```

---

## 十九、Clash Verge 与 Mihomo 的关系

可以理解为：

```text
Clash Verge / 类似桌面客户端
        ↓
GUI
        ↓
Mihomo / Clash Meta core
        ↓
代理节点、规则、TUN、System Proxy
```

Linux 服务器并不需要跑 Clash Verge GUI。

服务器直接运行：

```text
mihomo core
```

即可。

推荐分工：

```text
Windows：
Clash Verge / FlyBit GUI
负责桌面端

Linux：
mihomo core + systemd --user
负责服务器 CLI / VS Code Remote / Codex
```

两边不需要强行共用同一个进程或同一个实时节点选择。

---

## 二十、5 分钟快速恢复流程

以后 Codex 又无法联网时，按下面顺序：

### Step 1：Mihomo 服务

```bash
systemctl --user status mihomo
```

### Step 2：5964 监听者

```bash
ss -lntp | grep ':5964'
```

应看到：

```text
mihomo
```

### Step 3：代理变量

```bash
env | grep -i proxy
```

### Step 4：普通联网

```bash
curl -x http://127.0.0.1:5964 -s https://api.ipify.org
```

### Step 5：OpenAI API

```bash
curl -i https://api.openai.com/v1/models
```

看到 `401` 代表基本可达。

### Step 6：Codex CLI

```bash
codex
```

输入：

```text
hello
```

### Step 7：VS Code 扩展进程

```bash
ps -ef | grep 'openai.chatgpt.*codex' | grep xydeng | grep -v grep
```

然后：

```bash
tr '\0' '\n' < /proc/<PID>/environ | \
grep -iE 'http_proxy|https_proxy|all_proxy|no_proxy'
```

### Step 8：如果 Codex CLI 正常但扩展不正常

优先：

- 重启 VS Code Remote-SSH；
- 确认扩展进程环境变量；
- 不要先重装 Codex；
- 不要先改端口；
- 不要先恢复 SSH RemoteForward。

---

## 二十一、安全注意事项

以下内容不要提交到公开 GitHub：

```text
订阅 URL
subscription token
UUID
password
private-key
Reality private key / token
API Key
auth.json
Bearer Token
MFA 验证码
```

公开文档统一写：

```yaml
url: "<YOUR_SUBSCRIPTION_URL>"
uuid: "<REDACTED>"
token: "<REDACTED>"
```

如果真实订阅 URL、UUID、Token 曾粘贴到公开位置，应尽快在服务商处轮换。

---

## 二十二、当前最终状态

截至 2026-09-21：

```text
服务器：
192.168.30.205

用户：
xydeng

Mihomo：
/home/xydeng/.local/bin/mihomo

配置：
/home/xydeng/.config/mihomo/config.yaml

systemd：
mihomo.service
enabled + active

代理端口：
127.0.0.1:5964

Codex：
standalone 0.155.1

Codex 路径：
/home/xydeng/.local/bin/codex

OpenAI 路由：
OpenAI
→ ChatGPT-Auto
→ 仅 US / JP
→ https://api.openai.com/v1/models

普通流量：
PERSONAL_PROXY
→ https://www.gstatic.com/generate_204

SSH RemoteForward：
当前不使用
```

最终验证：

```text
Codex CLI：正常
VS Code Codex 环境变量：5964
Mihomo 5964：正常监听
OpenAI 专用分流：正常
```

---

## 二十三、最重要的经验总结

### 1. 普通网站最快 ≠ OpenAI 最稳定

不要把：

```text
Google generate_204 最快
```

直接等价成：

```text
ChatGPT / Codex 最稳定
```

### 2. 对 OpenAI 单独分流

推荐：

```text
OpenAI 域名
→ OpenAI 专用组
→ US / JP
→ OpenAI API 实际健康检查
```

### 3. 服务器尽量使用本地 Mihomo

相比：

```text
服务器
→ SSH
→ Windows
→ 桌面代理
```

更推荐：

```text
服务器
→ 本地 Mihomo
→ 代理节点
```

路径更短，状态更容易观察，也更适合长连接和流式响应。

### 4. 排查时做 A/B 测试

一次只改一个变量：

```text
同一 Codex
同一服务器
同一登录
只换代理路径
```

这样能快速判断到底是：

- Codex；
- 版本；
- 节点；
- SSH；
- Mihomo；
- VS Code 环境；

哪一层出了问题。

---

# 附录 A：`~/.bashrc` 推荐代理片段

```bash
export http_proxy='http://127.0.0.1:5964'
export https_proxy='http://127.0.0.1:5964'

export HTTP_PROXY="$http_proxy"
export HTTPS_PROXY="$https_proxy"

unset ALL_PROXY all_proxy

export no_proxy="api.deepseek.com"
export NO_PROXY="localhost,127.0.0.1,::1,api.deepseek.com"
```

---

# 附录 B：`~/.bash_profile`

```bash
# .bash_profile

if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

# Proxy settings are in ~/.bashrc
```

---

# 附录 C：`~/.profile`

```bash
# ~/.profile

if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi
```

---

# 附录 D：以后建议保留的诊断命令

```bash
# 代理
env | grep -i proxy

# Mihomo
systemctl --user status mihomo

# 端口
ss -lntp | grep ':5964'
sudo lsof -nP -iTCP:5964 -sTCP:LISTEN

# 出口
curl -x http://127.0.0.1:5964 -s https://api.ipify.org

# OpenAI
curl -i https://api.openai.com/v1/models

# Codex
codex --version
which codex
readlink -f "$(which codex)"
type -a codex

# VS Code Codex
ps -ef | grep 'openai.chatgpt.*codex' | grep xydeng | grep -v grep

# 进程环境
tr '\0' '\n' < /proc/<PID>/environ | \
grep -iE 'http_proxy|https_proxy|all_proxy|no_proxy'
```
