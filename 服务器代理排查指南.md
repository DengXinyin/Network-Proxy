# 服务器网络问题排查指南

> 适用环境：node4 服务器，用户 xydeng（uid 1012）
> 最后更新：2026-09-21
> 目的：下次遇到"连不上 ChatGPT / API 不通"不用再找人，照着这篇走一遍。

---

## 一、理解本机配置

### 加载链
```
~/.bash_profile  →  source ~/.bashrc
~/.profile       →  source ~/.bashrc   （VS Code Remote Server 也读这个）
```

所有代理和环境变量都在 `~/.bashrc`（第 37-49 行）里配置。

### 当前关键配置

```bash
# 代理（科学上网）
export http_proxy='http://127.0.0.1:5964'
export https_proxy='http://127.0.0.1:5964'
export HTTP_PROXY="$http_proxy"
export HTTPS_PROXY="$https_proxy"
unset ALL_PROXY all_proxy    # 清掉 all_proxy，避免干扰

# 不走代理的域名
export no_proxy="api.deepseek.com"

# DeepSeek API（Claude Code 后端）
export ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
export ANTHROPIC_AUTH_TOKEN="sk-..."
export ANTHROPIC_MODEL="deepseek-v4-flash"
```

> **注意**：`deepseek.com` 直连就行，所以加入 `no_proxy` 绕过代理。

---

## 二、快速自检（以后先跑这个）

### 1. 代理端口对不对？是不是活的？

```bash
# 查环境变量
echo "HTTP_PROXY=$HTTP_PROXY"
echo "HTTPS_PROXY=$HTTPS_PROXY"

# 查端口在不在监听
ss -tlnp | grep 5964
```

**期望输出**：
```
HTTP_PROXY=http://127.0.0.1:5964
LISTEN  0  128  127.0.0.1:5964  0.0.0.0:*  users:(("mihomo",pid=...,fd=3))
```

> 本机 mihomo 是 `bind-address: 127.0.0.1` + `allow-lan: false`，
> 所以**只有 IPv4 回环一行，没有 `[::1]`**。若看到 `[::1]:5964`，那是别的进程。

**不对怎么办**：
- 端口不存在 → 代理服务挂了，联系管理员重启 Clash/V2Ray
- 环境变量不对 → `source ~/.bashrc`

### 2. 代理能不能翻墙？

```bash
# 通用网站连通性
curl -sI --connect-timeout 5 --proxy http://127.0.0.1:5964 https://www.google.com | head -1

# 不走代理能不能通（对比）
curl -sI --connect-timeout 5 https://www.google.com 2>&1 | head -1
```

**判断**：
- 走代理返回 `HTTP/2 200` → ✅ 代理翻墙正常
- 走代理返回 `000` 或超时 → ❌ 代理挂了
- 直连返回 200 → 你不需要翻墙（你在境外）

### 3. OpenAI API 通不通？

```bash
# 用 fake key 测连通性（401 说明连上了，只是 key 不对）
curl -sv --connect-timeout 10 https://api.openai.com/v1/models \
  -H "Authorization: Bearer sk-test" 2>&1 | grep -E "< HTTP|ssl|Connected"

# 期望看到：
# < HTTP/2 401    ← 连上了！
# 如果看到 000 或 Connection refused → 不通
```

### 4. ChatGPT 网页能不能访问？

```bash
curl -sI --connect-timeout 10 --proxy http://127.0.0.1:5964 https://chatgpt.com 2>&1 | grep -E "^HTTP|cf-mitigated"
```

**判断**：
| 状态码 | 意思 |
|--------|------|
| `HTTP/2 403` + `cf-mitigated: challenge` | ✅ 代理通了，Cloudflare 反爬虫，**浏览器里正常** |
| `HTTP/2 200` | ✅ 完全正常 |
| 超时 / `000` | ❌ 代理不通 |
| `HTTP/2 403`（无 cf-mitigated） | 可能 IP 被 OpenAI 封了 |

---

## 三、问题分类排查

### A. "curl 通但浏览器/程序不通" → 环境变量没设对

```bash
# 确认当前 shell 是否设了代理
env | grep -i proxy

# 如果没设：
source ~/.bashrc

# 如果设了但端口不是 5964：
# 编辑 ~/.bashrc，把端口改成 5964
nano ~/.bashrc
```

**常见陷阱**：
- `ALL_PROXY` 设了不同的端口 → 会覆盖 `HTTP_PROXY`。本机已 `unset ALL_PROXY`
- Python 的 `requests` 库默认读 `http_proxy`（小写），所以小写也要设

### B. "全是超时 / 000" → 代理挂了或 DNS 污染

#### 先判断是不是 DNS 污染

```bash
# 用系统 DNS 查 chatgpt.com
nslookup chatgpt.com 2>&1 | grep "Address:"

# 同时用 dig 查（绕过系统 DNS）
dig chatgpt.com +short 2>&1
```

**判断 DNS 污染**：
- `nslookup` 返回陌生的 IP（不是 Cloudflare IP 段）→ **DNS 被污染**
- `nslookup` 和 `dig` 返回不同 IP → **DNS 被污染**
- 两者都返回域名不存在的错误 → DNS 服务器挂了

**Cloudflare 常见 IP 段**（chatgpt.com 用的是 Cloudflare）：
```
104.16.0.0 ~ 104.31.255.255
108.162.192.0 ~ 108.162.255.255
172.64.0.0 ~ 172.71.255.255
188.114.96.0 ~ 188.114.111.255
```

> 如果 DNS 返回的 IP 不在这些段里，大概率是假 IP。

#### DNS 污染了怎么修

**方案一（推荐）：用代理就行了，不用修 DNS**

因为 `HTTP_PROXY` 模式下，curl/程序把**域名**发给代理，代理自己解析 DNS，不受本地 DNS 污染影响。

验证：
```bash
# 这个用的是代理的 DNS，不受污染
curl -sI --proxy http://127.0.0.1:5964 https://chatgpt.com | head -1
```

**方案二：写死 /etc/hosts（临时）**

```bash
# 先通过代理获取真实 IP
curl -s --proxy http://127.0.0.1:5964 https://dns.google/resolve?name=chatgpt.com\&type=A 2>/dev/null | python3 -m json.tool 2>/dev/null | grep -E '"data"'

# 然后加到 /etc/hosts
sudo sh -c 'echo "真实IP chatgpt.com" >> /etc/hosts'
```

> 不推荐：Cloudflare IP 会变，hosts 写死过段时间可能又不行。

#### 真连不上代理怎么办

```bash
# 先确认代理端口在不在
ss -tlnp | grep 5964

# 如果端口没了 → 代理进程挂了，联系管理员或重启 Clash/V2Ray
# 如果端口在但连不上 → 可能代理服务出 bug，重启代理
```

### C. "返回 403 Forbidden" → Cloudflare 拦截

这是 **Cloudflare 的 bot 保护**，常见于 curl/wget 等命令行工具。

```bash
# 判断：看响应头有没有 cf-mitigated
curl -sI --proxy http://127.0.0.1:5964 https://chatgpt.com 2>&1 | grep "cf-mitigated"
```

**有 `cf-mitigated: challenge`** → Cloudflare 要求浏览器验证，curl 过不去。**这不是你的问题，API 不受影响。**

- ✅ `api.openai.com` 是 API，没有 Cloudflare 反爬，程序调用正常
- ❌ `chatgpt.com` 是网页，curl 被拦，**换浏览器访问**

### D. VS Code ChatGPT / Codex CLI 问题

#### 查看 codex 进程

```bash
ps aux | grep -i codex | grep $USER | grep -v grep
```

#### codex 进程说明

| 进程名 | 来源 | 作用 |
|--------|------|------|
| `codex -c ... app-server` | VS Code ChatGPT 扩展 | 远程会话的后端服务 |
| `codex app-server proxy` | VS Code ChatGPT 扩展 | SSH 代理转发 |
| `codex-code-mode-host` | VS Code ChatGPT 扩展 | Code 模式托管 |
| `codex` (前台) | 终端手动运行 | 交互式聊天 |

#### 批量杀掉所有 codex 进程

```bash
ps aux | grep -i codex | grep $USER | grep -v grep | awk '{print $2}' | xargs -r kill -9
```

#### 阻止 codex 自动重生

VS Code 的 ChatGPT 扩展会在远程连接时自动 spawn codex 进程。如果不想让它跑：

```bash
# 临时禁用（VS Code 找不到二进制就不会 spawn）
mv ~/.codex/packages/standalone/releases/*/bin/codex \
   ~/.codex/packages/standalone/releases/*/bin/codex.disabled

# 恢复
mv ~/.codex/packages/standalone/releases/*/bin/codex.disabled \
   ~/.codex/packages/standalone/releases/*/bin/codex
```

#### 终端手动启动 codex 聊天

```bash
# 前提：代理正常（端口 5964 监听中）
codex
```

> codex 需要真实 TTY，不能用 `nohup` 或在脚本里跑。

---

## 四、自检命令速查表

| 检查项 | 命令 | 正常结果 |
|--------|------|----------|
| 代理端口 | `ss -tlnp \| grep 5964` | `LISTEN` |
| 代理环境变量 | `echo $HTTPS_PROXY` | `http://127.0.0.1:5964` |
| Google 连通 | `curl -sI --proxy http://127.0.0.1:5964 https://www.google.com \| head -1` | `HTTP/2 200` |
| OpenAI API | `curl -s https://api.openai.com/v1/models -H "Authorization: Bearer sk-test" -w "%{http_code}" -o /dev/null` | `401` |
| ChatGPT 网页 | `curl -sI --proxy http://127.0.0.1:5964 https://chatgpt.com \| head -1` | `HTTP/2 403` |
| DNS 是否污染 | `nslookup chatgpt.com \| grep Address:` | IP 在 Cloudflare 段 |
| codex 进程 | `ps aux \| grep -i codex \| grep $USER \| grep -v grep` | 看情况 |
| 直连被墙 | `curl -sI --connect-timeout 5 https://www.google.com` | 超时 / 000 |

---

## 五、问题速查流程图

```
你的程序连不上 OpenAI API
        │
        ▼
① echo $HTTPS_PROXY
        │
   ┌────┴────┐
   │ 没输出   │  输出 http://127.0.0.1:5964
   └────┬────┘        │
        ▼              ▼
   source ~/.bashrc  ② ss -tlnp | grep 5964
        │                   │
        ▼              ┌────┴────┐
   重试即可          LISTEN   没 LISTEN
                       │         │
                       ▼         ▼
              ③ curl 测 API   代理挂了
                       │      联系管理员
                  ┌────┴────┐
              401 │         │ 000/超时
                  │         │
                  ▼         ▼
              ✅ 通了！  DNS污染/代理故障
              换真 key  → 见第三章 B 节
              就行
```

---

## 六、常用操作

### 让新终端自动加载代理

```bash
source ~/.bashrc
```

### 临时关代理

```bash
unset HTTP_PROXY HTTPS_PROXY http_proxy https_proxy
```

### 临时换代理端口（不改文件）

```bash
export HTTP_PROXY="http://127.0.0.1:5964"
export HTTPS_PROXY="http://127.0.0.1:5964"
```

### 测试某个 URL 走不走代理

```bash
# 走代理
curl -sI --proxy http://127.0.0.1:5964 https://目标网址 | head -3

# 不走代理
curl -sI --noproxy '*' https://目标网址 | head -3
```

### Python 程序用代理

```python
import os
import requests

# 方法一：读环境变量（推荐）
r = requests.get("https://api.openai.com/v1/models")

# 方法二：手动指定
r = requests.get(
    "https://api.openai.com/v1/models",
    proxies={"https": "http://127.0.0.1:5964"}
)
```

### 验证 API Key

```bash
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer sk-你的key" \
  -w "\nHTTP %{http_code}\n" -o /dev/null

# 200 = key 有效
# 401 = key 无效
# 000/超时 = 网络不通
```

---

## 七、已知问题 & 注意事项

1. **`~/.bashprofile` 不存在** — 你可能记错了，文件名是 `~/.bash_profile`。两者的区别：`~/.bash_profile` 是 login shell 读的，`~/.bashrc` 是每个新终端都读。现在所有配置都在 `~/.bashrc`。

2. **`ALL_PROXY` 不能设** — 如果 `ALL_PROXY` 指向别的端口，会覆盖 `HTTP_PROXY`/`HTTPS_PROXY`。当前配置已 `unset ALL_PROXY all_proxy`。

3. **`no_proxy` 别忘了** — `api.deepseek.com` 走直连，必须在 `no_proxy` 里。否则 Claude Code 的速度会被代理拖慢。

4. **端口变迁史：7892 → 7897 → 5964** — `~/.bashrc` 历史上指向过 7892（不可用）、7897（旧版指南记录的端口）。**当前正确值是 5964**，来源是本机 mihomo 配置 `~/.config/mihomo/config.yaml` 的 `mixed-port: 5964`。如果你发现环境变量变成别的值，说明 `.bashrc` 被回滚或被覆盖了。

   > ⚠️ 注意：`127.0.0.1:7897` 现在**仍在监听**，但进程属主不是 xydeng（`ss` 看不到属主），是**别人的**代理。别把环境变量指回 7897，否则等于蹭别人的代理，对方一停你就断网。

5. **VS Code 端口转发** — 如果你想把服务器上的代理 5964 转发到本地 Windows 电脑，在 VS Code 的 PORTS 面板添加 `5964`，然后在 Windows 浏览器里设代理为 `127.0.0.1:5964` 即可用服务器代理上网。

---

## 附录 A：`~/.bashrc`（当前生效配置）

```bash
# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific environment
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]
then
    PATH="$HOME/.local/bin:$HOME/bin:$PATH"
fi
export PATH

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=


# User specific aliases and functions

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/xydeng/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/xydeng/miniconda3/etc/profile.d/conda.sh" ]; then
       . "/home/xydeng/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/home/xydeng/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<


export http_proxy='http://127.0.0.1:5964'
export https_proxy='http://127.0.0.1:5964'
export HTTP_PROXY="$http_proxy"
export HTTPS_PROXY="$https_proxy"
unset ALL_PROXY all_proxy

export no_proxy="api.deepseek.com"
export ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
export ANTHROPIC_AUTH_TOKEN="sk-..."   # 真实值见 ~/.bashrc，勿写入文档
export ANTHROPIC_MODEL="deepseek-v4-flash"
export ANTHROPIC_DEFAULT_OPUS_MODEL="deepseek-v4-flash"
export ANTHROPIC_DEFAULT_SONNET_MODEL="deepseek-v4-flash"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="deepseek-v4-flash"
```

## 附录 B：`~/.bash_profile`

```bash
# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs
# Proxy settings are in ~/.bashrc (sourced above), do NOT override here
```

## 附录 C：`~/.profile`

```bash
# ~/.profile - sourced by login shells and VS Code Remote Server

# Source bashrc for consistent environment
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi
```
