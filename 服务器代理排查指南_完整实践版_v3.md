# 服务器网络代理与 OpenAI/Codex 排查指南（完整实践版）

> 适用场景：Windows / macOS 本地代理 + Linux 服务器 + VS Code Remote-SSH + Mihomo / Clash Verge + OpenAI Codex  
> 最后更新：2026-09-21（补充 Clash 本地客户端 → Linux Mihomo 从零安装与迁移）  
> 目标：如果以后再次遇到“本地能访问、服务器不能访问”“Codex 403 / 断流”“代理端口混乱”“VS Code 扩展和终端表现不一致”等问题，可以只看本文，从零检查、搭建、验证和修复。

---

# 0. 先记住最终结论

这次排障最终确认了一个非常重要的事实：

> **“普通网站测速最快的代理节点”不等于“OpenAI / ChatGPT / Codex 最稳定的代理节点”。**

原始 Mihomo 策略使用：

```text
所有节点
→ 用 Google generate_204 测速
→ 自动选择延迟最低节点
```

这会导致：

- 普通网页访问正常；
- `curl` 正常；
- Google 正常；
- 但 OpenAI / ChatGPT / Codex 可能 403、断流或长连接异常；
- 自动选中的节点可能落到并不适合 OpenAI 的地区或线路。

最终稳定方案改成：

```text
OpenAI / ChatGPT 域名
→ OpenAI 专用策略
→ 只筛选 US / JP 节点
→ 用 https://api.openai.com/v1/models 做实际健康检查
→ 自动选择对 OpenAI 更合适的节点
```

其他普通网站仍然使用通用测速策略。

---

# 1. 最终推荐网络结构

推荐长期使用：

```text
Windows / macOS
└─ Clash Verge / FlyBit / 其他桌面代理
   └─ 负责本机桌面网络

Linux 服务器
└─ 127.0.0.1:5964
   └─ 自己的 Mihomo
      ├─ OpenAI / ChatGPT / Codex
      │  └─ OpenAI
      │     └─ ChatGPT-Auto
      │        └─ US / JP 节点
      │           └─ OpenAI API 健康检查
      │
      └─ 其他网络
         └─ PERSONAL_PROXY
            └─ 通用延迟自动选择
```

当前正式服务器代理端口：

```text
127.0.0.1:5964
```

推荐服务器自己运行 Mihomo，而不是长期依赖：

```text
Linux 服务器
→ SSH RemoteForward
→ Windows
→ Windows 本地代理
```

后者可以用于临时测试，但链路更长，对 Codex streaming / 长连接不一定稳定。

---

# 2. 最容易混淆的概念：127.0.0.1 和端口

## 2.1 `127.0.0.1` 永远代表“当前这台机器自己”

Windows：

```text
127.0.0.1:5964
```

代表 Windows 自己的 5964。

Linux：

```text
127.0.0.1:5964
```

代表 Linux 服务器自己的 5964。

它们虽然数字一样，但不是同一个端口。

因此：

```text
Windows 127.0.0.1:5964
Linux   127.0.0.1:5964
```

可以同时存在，而且由完全不同的程序监听。

## 2.2 判断端口是谁占用

Linux：

```bash
ss -lntp | grep ':5964'
```

如果看不到 PID：

```bash
sudo ss -lntp | grep ':5964'
```

或者：

```bash
sudo lsof -nP -iTCP:5964 -sTCP:LISTEN
```

Windows PowerShell：

```powershell
Get-NetTCPConnection -LocalPort 5964 -State Listen
```

查看对应 PID：

```powershell
Get-Process -Id <PID>
```

也可以：

```powershell
netstat -ano | findstr :5964
```

macOS：

```bash
lsof -nP -iTCP:5964 -sTCP:LISTEN
```

---

# 3. 本地电脑：如何检查代理端口、出口 IP、实际地区

以后换电脑、换 Windows、换 Mac，都可以先做这一节。

## 3.1 Windows PowerShell

假设本地代理监听：

```text
127.0.0.1:5964
```

先确认端口：

```powershell
Get-NetTCPConnection -LocalPort 5964 -State Listen
```

或者：

```powershell
netstat -ano | findstr :5964
```

然后测试实际出口 IP：

```powershell
curl.exe -x http://127.0.0.1:5964 -s https://api.ipify.org
```

例如：

```text
23.142.132.80
```

查询大概地区：

```powershell
curl.exe -x http://127.0.0.1:5964 -s https://ipinfo.io
```

如果 `ipinfo.io` 429，可以换：

```powershell
curl.exe -x http://127.0.0.1:5964 -s https://ipapi.co/json/
```

注意：

> IP 地理位置是第三方数据库推断，只能作为大概参考，不保证精确到城市。

## 3.2 macOS Terminal

确认端口：

```bash
lsof -nP -iTCP:5964 -sTCP:LISTEN
```

测试代理出口：

```bash
curl -x http://127.0.0.1:5964 -s https://api.ipify.org
```

查看大致地区：

```bash
curl -x http://127.0.0.1:5964 -s https://ipinfo.io
```

或者：

```bash
curl -x http://127.0.0.1:5964 -s https://ipapi.co/json/
```

---

# 4. 本地和服务器：如何确认是不是同一个代理出口

这是判断“服务器是否真的走了本地代理”的最简单方法。

假设：

```text
Windows 本地代理：127.0.0.1:5964
服务器代理入口：127.0.0.1:17897
```

Windows：

```powershell
curl.exe -x http://127.0.0.1:5964 -s https://api.ipify.org
```

服务器：

```bash
curl -x http://127.0.0.1:17897 -s https://api.ipify.org
```

如果两边都是：

```text
23.142.132.80
```

说明：

```text
服务器
→ SSH RemoteForward
→ Windows 5964
→ 相同代理出口
```

如果服务器自己运行 Mihomo，则：

```bash
curl -x http://127.0.0.1:5964 -s https://api.ipify.org
```

这里不要求和 Windows IP 相同，因为服务器已经使用自己的节点策略。

---

# 5. 两种代理架构：RemoteForward 与服务器本地 Mihomo

## 5.1 方案 A：SSH RemoteForward

结构：

```text
Linux 程序
→ Linux 127.0.0.1:17897
→ SSH 隧道
→ Windows 127.0.0.1:5964
→ Windows 代理
→ Internet
```

Windows SSH config：

```sshconfig
Host 192.168.30.205
  HostName 192.168.30.205
  User xydeng
  Port 22
  RemoteForward 17897 127.0.0.1:5964
  ExitOnForwardFailure yes
```

服务器设置：

```bash
export HTTP_PROXY=http://127.0.0.1:17897
export HTTPS_PROXY=http://127.0.0.1:17897
export http_proxy=http://127.0.0.1:17897
export https_proxy=http://127.0.0.1:17897
```

检查：

```bash
ss -lntp | grep ':17897'
```

测试：

```bash
curl -x http://127.0.0.1:17897 -s https://api.ipify.org
```

### RemoteForward 的典型错误

```text
remote port forwarding failed for listen port 17897
```

通常意味着服务器 `17897` 已经被其他进程或旧 SSH 会话占用。

检查：

```bash
sudo lsof -nP -iTCP:17897 -sTCP:LISTEN
```

再看 PID：

```bash
ps -fp <PID>
```

### RemoteForward 适用场景

适合：

- 临时借本地 Windows 代理；
- 服务器暂时无法安装代理核心；
- 快速排障。

不推荐长期用于：

- Codex streaming；
- 长连接；
- 对稳定性要求高的 API。

## 5.2 方案 B：服务器本地 Mihomo

结构：

```text
Linux 程序
→ 127.0.0.1:5964
→ Linux 本地 Mihomo
→ 代理节点
→ Internet
```

优点：

```text
路径短
状态容易检查
不依赖 Windows
不依赖 SSH 隧道
VS Code Remote / CLI / Python 都可以统一使用
更适合 Codex streaming
```

本次最终采用的就是这个方案。

---

# 6. Clash Verge / 桌面代理 / Mihomo 到底是什么关系

可以简单理解为：

```text
Clash Verge / FlyBit / 类似 GUI
        ↓
桌面界面
        ↓
Mihomo / Clash Meta core
        ↓
代理节点、规则、DNS、TUN、System Proxy
```

在 Windows / macOS 上通常是：

```text
GUI + Mihomo core
```

在 Linux 服务器上只需要：

```text
Mihomo core
```

不需要 GUI。

---

# 7. 本地用 Clash for Windows / Clash Verge，但服务器没有 GUI：服务器到底要装什么？

这是一个非常重要的概念。

如果 Windows 或 macOS 本地正在使用：

```text
Clash for Windows
Clash Verge
FlyBit
其他基于 Clash / Mihomo 的桌面客户端
```

这些桌面软件通常负责：

```text
订阅管理
节点选择
规则管理
系统代理
TUN
GUI
        ↓
底层代理核心
        ↓
Mihomo / Clash Meta
```

但 Linux 服务器通常不需要、也不建议安装完整桌面 GUI。

服务器真正需要的只是：

```text
Mihomo core
+
config.yaml
+
订阅 / provider 文件
+
systemd 服务
+
HTTP_PROXY / HTTPS_PROXY 环境变量
```

也就是说：

```text
Windows 本地：
Clash GUI → 本地 Mihomo core → 节点 → Internet

Linux 服务器：
无 GUI
Mihomo core → config.yaml → 节点 → Internet
```

两边可以使用同一个代理服务商、同一个订阅，但它们是**两个独立运行的代理实例**。

服务器不会自动继承：

```text
Windows 当前选中的节点
Windows Clash GUI 当前策略
Windows Clash 当前运行状态
```

除非使用 SSH RemoteForward 把服务器流量明确送回 Windows。

当前推荐方案不是这么做，而是：

```text
本地电脑自己运行自己的 Clash/Mihomo
服务器自己运行自己的 Mihomo
```

---

## 7.1 本地 Clash 和服务器 Mihomo 的关系

假设你在 Windows Clash 中导入了一条订阅。

Windows：

```text
订阅 URL
   ↓
Clash for Windows / Clash Verge
   ↓
本地 Mihomo
   ↓
127.0.0.1:5964
```

服务器如果也要使用这条订阅，推荐：

```text
同一条订阅 URL
   ↓
服务器 ~/.config/mihomo/config.yaml
   ↓
proxy-provider
   ↓
服务器自己的 Mihomo
   ↓
服务器 127.0.0.1:5964
```

注意：

> 服务器不是“调用 Windows 的 Mihomo”，而是“自己运行一份 Mihomo，并独立加载订阅”。

因此本地 Windows 关机后，服务器代理仍然可以继续工作。

---

## 7.2 什么时候才需要 Windows → 服务器的 SSH RemoteForward

只有在服务器无法自己运行代理时，才临时采用：

```text
服务器
→ SSH RemoteForward
→ Windows Clash
→ Internet
```

例如：

```sshconfig
RemoteForward 17897 127.0.0.1:5964
```

服务器使用：

```bash
export HTTPS_PROXY=http://127.0.0.1:17897
```

这是“借 Windows 的代理”。

而服务器本地 Mihomo：

```text
服务器
→ 服务器 Mihomo
→ 代理节点
→ Internet
```

是“服务器自己有代理”。

两者是完全不同的架构。

---

# 8. 从零安装 Linux Mihomo Core

官方 Mihomo 提供 Linux 预编译二进制。新服务器上不需要安装 Clash GUI，只需要下载对应 CPU 架构的 Mihomo 可执行文件。

官方项目：

```text
https://github.com/MetaCubeX/mihomo
```

官方 Release：

```text
https://github.com/MetaCubeX/mihomo/releases/latest
```

---

## 8.1 第一步：确认 Linux CPU 架构

执行：

```bash
uname -m
```

常见结果：

```text
x86_64
```

对应：

```text
linux-amd64
```

如果：

```text
aarch64
```

对应：

```text
linux-arm64
```

如果服务器比较老，还建议看：

```bash
uname -r
```

避免下载对旧 Linux 内核不兼容的构建。

---

## 8.2 第二步：创建目录

推荐所有东西都装在自己的 HOME 下，不影响其他用户：

```bash
mkdir -p ~/.local/bin
mkdir -p ~/.config/mihomo/providers
mkdir -p ~/.config/systemd/user
```

目录最终类似：

```text
~/.local/bin/
└── mihomo

~/.config/mihomo/
├── config.yaml
├── cache.db
├── api.sock
└── providers/
    └── personal_subscription.yaml

~/.config/systemd/user/
└── mihomo.service
```

---

## 8.3 第三步：下载 Mihomo

去官方 Releases 页面找到与你的系统匹配的 Linux 构建。

例如：

```text
x86_64 → linux-amd64
ARM64  → linux-arm64
```

如果下载的是 `.gz` 文件，可以：

```bash
curl -L "<OFFICIAL_MIHOMO_RELEASE_ASSET_URL>" \
  -o /tmp/mihomo.gz
```

解压：

```bash
gzip -dc /tmp/mihomo.gz > ~/.local/bin/mihomo
```

增加执行权限：

```bash
chmod +x ~/.local/bin/mihomo
```

验证：

```bash
~/.local/bin/mihomo -v
```

如果能输出版本信息，说明 Mihomo core 已经安装。

---

## 8.4 如果服务器自己暂时上不了 GitHub 怎么办

这是一个常见的“鸡生蛋”问题：

```text
要下载 Mihomo
→ 但服务器还没有代理
→ GitHub 下载失败
```

可以在 Windows / macOS 本地下载 Mihomo 二进制，然后通过 SCP 上传。

Windows PowerShell：

```powershell
scp .\mihomo-linux-amd64*.gz \
  xydeng@192.168.30.205:/home/xydeng/
```

macOS / Linux：

```bash
scp ./mihomo-linux-amd64*.gz \
  xydeng@192.168.30.205:/home/xydeng/
```

服务器：

```bash
gzip -dc ~/mihomo-linux-amd64*.gz > ~/.local/bin/mihomo
chmod +x ~/.local/bin/mihomo
~/.local/bin/mihomo -v
```

这样第一次安装不需要服务器先有代理。

---

# 9. 如何把本地 Clash 的订阅“带到”服务器

服务器 Mihomo 需要节点来源。

推荐优先级：

```text
方法 A：服务器直接使用订阅 URL
方法 B：从本地导出 provider YAML 再上传服务器
```

---

## 9.1 方法 A：直接在服务器配置订阅 URL（推荐）

`~/.config/mihomo/config.yaml`：

```yaml
proxy-providers:
  personal_subscription:
    type: http

    url: "<YOUR_SUBSCRIPTION_URL>"

    path: ./providers/personal_subscription.yaml

    interval: 3600

    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
```

启动 Mihomo 后，它会自行下载：

```text
订阅 URL
→ ~/.config/mihomo/providers/personal_subscription.yaml
```

检查：

```bash
ls -lh ~/.config/mihomo/providers/
```

---

## 9.2 方法 B：本地先下载 provider，再上传服务器

如果订阅地址在服务器上暂时访问不了，可以从本地客户端或本地浏览器获取 provider YAML。

然后 Windows：

```powershell
scp .\personal_subscription.yaml \
  xydeng@192.168.30.205:/home/xydeng/.config/mihomo/providers/
```

macOS：

```bash
scp ./personal_subscription.yaml \
  xydeng@192.168.30.205:/home/xydeng/.config/mihomo/providers/
```

此时 `config.yaml` 仍然引用：

```yaml
path: ./providers/personal_subscription.yaml
```

如果长期只想使用静态 provider，可以避免自动远程更新。

但更推荐最终恢复 `type: http + url:`，便于订阅自动更新。

---

# 10. 不建议直接复制 Clash for Windows 整个配置目录

不要简单把：

```text
Windows Clash 配置目录
```

整个复制到 Linux。

因为里面可能包含：

```text
Windows 路径
GUI 状态
缓存
系统代理设置
TUN 配置
Windows 专用字段
客户端私有配置
```

推荐做法是只迁移真正需要的内容：

```text
订阅 URL
节点 provider
proxy-groups
rules
DNS 策略（必要时）
```

然后单独写一份服务器专用：

```text
~/.config/mihomo/config.yaml
```

---

# 11. 服务器 Mihomo 最小可运行配置

如果只是为了验证“服务器能不能成功跑 Mihomo”，可以先用一个最小配置。

```yaml
mixed-port: 5964
allow-lan: false
bind-address: 127.0.0.1

mode: rule
log-level: info
ipv6: false

proxy-providers:
  personal_subscription:
    type: http
    url: "<YOUR_SUBSCRIPTION_URL>"
    path: ./providers/personal_subscription.yaml
    interval: 3600

proxy-groups:
  - name: PROXY
    type: url-test
    use:
      - personal_subscription
    url: https://www.gstatic.com/generate_204
    interval: 300

rules:
  - MATCH,PROXY
```

先验证它能跑。

前台启动：

```bash
~/.local/bin/mihomo -d ~/.config/mihomo
```

另开一个终端：

```bash
ss -lntp | grep ':5964'
```

测试：

```bash
curl -x http://127.0.0.1:5964 \
  -s https://api.ipify.org
```

如果能返回公网 IP，说明 Mihomo 基础环境正常。

然后再升级到后面的 OpenAI 专用配置。

---

# 12. OpenAI 优化版 Mihomo 配置

文件：

```text
~/.config/mihomo/config.yaml
```

推荐：

```yaml
mixed-port: 5964
allow-lan: false
bind-address: 127.0.0.1

mode: rule
log-level: info
ipv6: false

external-controller-unix: /home/<USER>/.config/mihomo/api.sock

proxy-providers:
  personal_subscription:
    type: http

    url: "<YOUR_SUBSCRIPTION_URL>"

    path: ./providers/personal_subscription.yaml

    interval: 3600

    health-check:
      enable: true
      interval: 300
      timeout: 5000
      url: https://www.gstatic.com/generate_204

proxy-groups:

  # OpenAI 专用自动节点组
  - name: ChatGPT-Auto
    type: url-test

    use:
      - personal_subscription

    # 第一步：只筛美国和日本
    filter: '(?i)(美国|日本|united states|japan|(^|[^a-z])(us|jp)([^a-z]|$))'

    # 第二步：直接使用 OpenAI API 检查这些节点
    url: https://api.openai.com/v1/models

    interval: 300
    tolerance: 150
    lazy: false

  # 普通网站
  - name: PERSONAL_PROXY
    type: url-test

    use:
      - personal_subscription

    url: https://www.gstatic.com/generate_204

    interval: 300
    tolerance: 80
    lazy: false

  # OpenAI 最终策略入口
  - name: OpenAI
    type: select

    proxies:
      - ChatGPT-Auto
      - PERSONAL_PROXY

rules:

  - DOMAIN,chatgpt.com,OpenAI
  - DOMAIN,auth.openai.com,OpenAI

  - DOMAIN-SUFFIX,chatgpt.com,OpenAI
  - DOMAIN-SUFFIX,openai.com,OpenAI
  - DOMAIN-SUFFIX,oaistatic.com,OpenAI
  - DOMAIN-SUFFIX,oaiusercontent.com,OpenAI
  - DOMAIN-SUFFIX,statsigapi.net,OpenAI

  - MATCH,PERSONAL_PROXY
```

---

# 13. 为什么这套 OpenAI 策略比通用自动测速更合适

旧策略：

```text
所有节点
↓
Google generate_204
↓
选最低延迟
```

问题：

```text
Google 快
≠
OpenAI 一定稳定
```

新策略：

```text
所有节点
↓
先 filter
↓
US / JP
↓
api.openai.com/v1/models
↓
自动选择
```

也就是说：

```text
先限定候选国家
再针对目标服务测试
```

而不是：

```text
让所有网站共用一个“通用最快节点”
```

这就是本次问题最终解决的关键。

---

# 14. 给服务器建立 systemd 用户服务

文件：

```text
~/.config/systemd/user/mihomo.service
```

内容：

```ini
[Unit]
Description=xydeng personal mihomo proxy
After=network-online.target

[Service]
Type=simple

ExecStart=%h/.local/bin/mihomo -d %h/.config/mihomo

Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
```

重新加载：

```bash
systemctl --user daemon-reload
```

启用并立即启动：

```bash
systemctl --user enable --now mihomo
```

查看：

```bash
systemctl --user status mihomo
```

确认端口：

```bash
ss -lntp | grep ':5964'
```

预期：

```text
127.0.0.1:5964
users:(("mihomo",pid=...,fd=...))
```

---

# 15. 如果希望退出 SSH 后 Mihomo 仍长期运行

用户级 systemd 服务是否在退出登录后继续运行，取决于系统登录管理策略。

可以检查：

```bash
loginctl show-user "$USER" | grep Linger
```

如果需要在没有登录会话时也长期运行，可让管理员执行：

```bash
sudo loginctl enable-linger xydeng
```

之后：

```bash
loginctl show-user xydeng | grep Linger
```

预期：

```text
Linger=yes
```

如果你没有管理员权限，可以让服务器管理员配置。

---

# 16. 安装完成后的完整验收

## 16.1 服务

```bash
systemctl --user status mihomo
```

## 16.2 端口

```bash
ss -lntp | grep ':5964'
```

## 16.3 普通网络

```bash
curl -x http://127.0.0.1:5964 \
  -s https://api.ipify.org
```

## 16.4 OpenAI API

```bash
curl -i \
  -x http://127.0.0.1:5964 \
  https://api.openai.com/v1/models
```

看到：

```text
401
```

通常代表：

```text
网络已成功到 OpenAI
只是没有 Authorization
```

## 16.5 Codex

配置环境：

```bash
export HTTP_PROXY=http://127.0.0.1:5964
export HTTPS_PROXY=http://127.0.0.1:5964
export http_proxy=http://127.0.0.1:5964
export https_proxy=http://127.0.0.1:5964
```

然后：

```bash
codex
```

输入：

```text
hello
```

只有这一步才能最终确认：

```text
OpenAI streaming
Codex 长连接
真实登录请求
```

全部正常。

---

# 17. 这个 OpenAI 策略到底怎么选节点

顺序是：

```text
订阅中的所有节点
        ↓
filter
        ↓
先只留下 US / JP
        ↓
url-test
        ↓
用 api.openai.com/v1/models 测试
        ↓
在 US / JP 中自动选一个当前最合适的节点
```

不是：

```text
先从全部节点自动选
→ 再判断国家
```

而是：

```text
先限制国家
→ 再自动选
```

---

# 18. 为什么 `api.ipify.org` 显示香港，但 Codex 可能仍然正常

因为 Mihomo 是按域名分流。

执行：

```bash
curl -x http://127.0.0.1:5964 -s https://api.ipify.org
```

这个域名不属于 OpenAI，所以走：

```text
PERSONAL_PROXY
```

而 Codex 请求：

```text
chatgpt.com
api.openai.com
```

会走：

```text
OpenAI
→ ChatGPT-Auto
→ US / JP
```

所以：

> 普通网站出口 IP 和 OpenAI 实际出口 IP 可以不同。

---

# 19. 如何查看 OpenAI 当前实际选中了哪个节点

前提：

```yaml
external-controller-unix: /home/<USER>/.config/mihomo/api.sock
```

先检查：

```bash
ls -l ~/.config/mihomo/api.sock
```

查询：

```bash
curl --unix-socket ~/.config/mihomo/api.sock   http://localhost/proxies/ChatGPT-Auto
```

如果有 `jq`：

```bash
curl -s --unix-socket ~/.config/mihomo/api.sock   http://localhost/proxies/ChatGPT-Auto | jq
```

没有 `jq`：

```bash
curl -s --unix-socket ~/.config/mihomo/api.sock   http://localhost/proxies/ChatGPT-Auto | python3 -m json.tool
```

查询最终 OpenAI 组：

```bash
curl -s --unix-socket ~/.config/mihomo/api.sock   http://localhost/proxies/OpenAI | python3 -m json.tool
```

---

# 20. 用 systemd 管理 Mihomo

文件：

```text
~/.config/systemd/user/mihomo.service
```

内容：

```ini
[Unit]
Description=Personal Mihomo Proxy
After=network-online.target

[Service]
Type=simple
ExecStart=%h/.local/bin/mihomo -d %h/.config/mihomo
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
```

应用：

```bash
systemctl --user daemon-reload
systemctl --user enable --now mihomo
```

查看：

```bash
systemctl --user status mihomo
```

重启：

```bash
systemctl --user restart mihomo
```

日志：

```bash
journalctl --user -u mihomo -n 100 --no-pager
```

---

# 21. 修改配置后的标准验证顺序

第一步：

```bash
systemctl --user restart mihomo
```

第二步：

```bash
systemctl --user status mihomo
```

第三步：

```bash
ss -lntp | grep ':5964'
```

第四步：

```bash
curl -x http://127.0.0.1:5964 -s https://api.ipify.org
```

第五步：

```bash
curl -i https://api.openai.com/v1/models
```

看到：

```text
401 Missing bearer authentication
```

通常表示 OpenAI 网络已可达。

最后：

```bash
codex
```

输入：

```text
hello
```

只有真实聊天才能验证 streaming 是否稳定。

---

# 22. 推荐先用测试端口，不直接改正式 5964

复制：

```bash
cp -a ~/.config/mihomo ~/.config/mihomo-test
```

测试配置改：

```yaml
mixed-port: 5965
```

启动：

```bash
~/.local/bin/mihomo -d ~/.config/mihomo-test
```

确认：

```bash
ss -lntp | grep ':5965'
```

临时切换当前 shell：

```bash
export HTTP_PROXY=http://127.0.0.1:5965
export HTTPS_PROXY=http://127.0.0.1:5965
export http_proxy=http://127.0.0.1:5965
export https_proxy=http://127.0.0.1:5965
```

测试：

```bash
codex
```

输入：

```text
hello
```

正常后再迁回 5964。

---

# 23. Linux Shell：`.bashrc` / `.bash_profile` / `.profile`

## 15.1 `~/.bashrc`

```bash
export http_proxy='http://127.0.0.1:5964'
export https_proxy='http://127.0.0.1:5964'

export HTTP_PROXY="$http_proxy"
export HTTPS_PROXY="$https_proxy"

unset ALL_PROXY all_proxy

export no_proxy="localhost,127.0.0.1,::1,api.deepseek.com"
export NO_PROXY="$no_proxy"
```

如果还有 Claude Code：

```bash
export ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
export ANTHROPIC_AUTH_TOKEN="<REDACTED>"
export ANTHROPIC_MODEL="deepseek-v4-flash"
```

加载：

```bash
source ~/.bashrc
```

## 15.2 `~/.bash_profile`

```bash
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi
```

## 15.3 `~/.profile`

```bash
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi
```

---

# 24. VS Code Remote-SSH：终端正常，扩展仍可能不正常

找 Codex：

```bash
ps -ef | grep 'openai.chatgpt.*codex' | grep "$USER" | grep -v grep
```

拿到 PID：

```bash
tr ' ' '
' < /proc/<PID>/environ | grep -iE 'http_proxy|https_proxy|all_proxy|no_proxy'
```

预期：

```text
HTTP_PROXY=http://127.0.0.1:5964
HTTPS_PROXY=http://127.0.0.1:5964
http_proxy=http://127.0.0.1:5964
https_proxy=http://127.0.0.1:5964
```

如果不是，重启 VS Code Remote-SSH。

---

# 25. 当前 Windows SSH config 推荐

```sshconfig
Host 192.168.30.206
  HostName 192.168.30.206
  User xydeng
  Port 22

Host 192.168.30.205
  HostName 192.168.30.205
  User xydeng
  Port 22
  ServerAliveInterval 30
  ServerAliveCountMax 3
  # IdentityFile C:/Users/HUITIAN/.ssh/id_rsa
```

不再长期启用：

```sshconfig
# RemoteForward 5964 127.0.0.1:5964
# ExitOnForwardFailure yes
```

---

# 26. Codex CLI：版本和路径

```bash
codex --version
```

当前：

```text
codex-cli 0.155.1
```

查看：

```bash
which codex
readlink -f "$(which codex)"
type -a codex
```

当前 standalone 路径：

```text
/home/xydeng/.codex/packages/standalone/releases/0.155.1-x86_64-unknown-linux-musl/bin/codex
```

---

# 27. 状态码速查

| 状态 | 含义 |
|---|---|
| `Connection refused` | 本地端口没人监听 |
| `remote port forwarding failed` | SSH 远程端口被占用或无法绑定 |
| `401 Missing bearer authentication` | OpenAI API 已可达，但没认证 |
| `403 + cf-mitigated` | Cloudflare 浏览器挑战 |
| `405 Method Not Allowed` | endpoint 可达，但请求方法错误 |
| `429 Rate limit hit` | 测试网站本身限流 |
| `stream disconnected before completion` | 长连接 / streaming 中断 |
| CLI 正常、VS Code 不正常 | 常见于扩展继承旧代理变量 |

---

# 28. 本次问题的关键 A/B 测试

```text
同一个服务器
同一个 Codex
同一个账号
同一个模型
只换代理路径
```

结果：

```text
SSH RemoteForward → Windows FlyBit
Codex streaming 失败

服务器另一个本地 Mihomo
Codex 正常

自己的 Mihomo + OpenAI 专用规则
Codex 正常
```

因此确认：

```text
不是 Codex 版本问题
不是账号问题
不是 Linux 问题
不是 OpenAI 服务问题
```

核心问题是：

```text
原代理节点策略 + SSH 中转链路不适合 Codex 长连接
```

---

# 29. 新机器复制整套配置时的标准流程

```text
① 确定本地电脑代理端口
② 本地测试实际公网 IP / 地区
③ 登录服务器
④ 检查服务器端口冲突
⑤ 安装 Mihomo
⑥ 建 ~/.config/mihomo/config.yaml
⑦ 建 systemd user service
⑧ 启动 Mihomo
⑨ 配置 ~/.bashrc
⑩ source ~/.bashrc
⑪ curl 测普通网络
⑫ curl 测 OpenAI API
⑬ codex 实测
⑭ 检查 VS Code 扩展进程环境
```

---

# 30. 安全注意事项

以下内容不要上传到公开 GitHub：

```text
订阅 URL
subscription token
UUID
password
Reality 私钥 / token
API Key
auth.json
Bearer Token
MFA 验证码
```

公开文档写：

```yaml
url: "<YOUR_SUBSCRIPTION_URL>"
uuid: "<REDACTED>"
token: "<REDACTED>"
```

---

# 31. 一句话总结

服务器代理排障最重要的不是“有没有代理”，而是分三层检查：

```text
第一层：端口是否真的监听
第二层：程序是否真的使用这个代理
第三层：这个代理策略是否真的适合目标网站
```

针对 OpenAI / Codex，长期推荐：

```text
服务器本地 Mihomo
+ OpenAI 域名独立分流
+ US / JP 候选节点
+ OpenAI API 实际健康检查
+ systemd 自动管理
+ VS Code 进程环境验证
```

这套比“所有流量统一走通用最快节点”更可靠，也比“服务器长期借 Windows 的 SSH RemoteForward”更容易维护。
